#!/usr/bin/env groovy

@Library('lib')_

helmchartPipeline{
  chart_path       = './chart'
  chart_repository = 'helm-public'
  downstream_job = 'openEO/openeo-workspace-service-terraform-kubernetes-deploy-dev'
}